let newUserData = {
    userID : user.uid,                
    email : user.email,
    phone: user.phoneNumber,                
    date : new Date(),
    'uid': uid,
        'name': name,
        'address': address
}